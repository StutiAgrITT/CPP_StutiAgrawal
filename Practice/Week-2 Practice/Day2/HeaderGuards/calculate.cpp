#include "calculations.h"
#include "calculations.h" //if header file doesnt have guards
                          //it will give redifinition errors ()

int main() {
    Add(10, 5);
    Subtract(10, 5);
    int square = SQUARE(3 + 2);
}